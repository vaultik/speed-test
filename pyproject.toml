[project]
name = "speed-test"
version = "0.1.0"
description = "A small Python script for checking download speed for a given URL"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "requests>=2.34.2",
]
